
remdep ( [xi, eta, d[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 2, 15
#

remdep ( [xi, eta, b[1], a[2]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 2, 25
#

remdep ( [xi, eta] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 2, 26
#

remdep ( [xi, eta], a[1], c[1], d[1]  );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 10
#

remdep ( [xi, eta] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 13
#

remdep ( [xi, eta, d[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 16
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 3, 17, 1
#
remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 3, 17, 2
#
#
remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 3, 17, 3
#
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 3, 17, 4
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 3, 17
#

remdep ( [xi, eta, c[1], b[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 20
#

remdep ( [xi, eta] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 24
#

remdep ( [sigma, rho, xi, eta, c[1], b[1]] );

depend ( [sigma, rho, xi, eta], [x, y] );

remdepcoeflist ();

###########################################################################
#
# 	Gruppe g 3, 5

remdep ( [sigma, rho, xi, eta, c[1], b[1]] );

depend ( [sigma, rho, xi, eta], [x, y] );

remdepcoeflist ();

###########################################################################
#
# 	Gruppe g 3, 7

remdep ( [sigma, rho, xi, eta, c[1], d[1]] );

depend ( [sigma, rho, xi, eta], [x, y] );

remdepcoeflist ();

###########################################################################
#
# 	Gruppe g 3, 7

remdep ( [xi, eta] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 3, 8
#

remdep ( [xi, eta, c[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 4, 14
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 1
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 2
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 3
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 4
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 5
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Group g 4, 17, 6
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 4, 17, 7
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 4, 17
#

remdep ( [xi, eta, sigma, rho] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 4, 18, 1
#

remdep ( [xi, eta, sigma, rho, d[1]] );

depend ( [xi, eta, sigma, rho], [x,y] );


####################################################################################
#
#   Groups g 4, 18, 2
#

remdep ( [xi, eta, d[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 4, 20
#

remdep ( [sigma, rho, xi, eta, e[1]] );

depend ( [sigma, rho, xi, eta], [x, y] );

remdepcoeflist ();

###########################################################################
#
# 	Gruppe g 4, 6

remdep ( [xi, eta, c[1], d[1], b[1]] );

depend ( [xi,eta], [x,y] );


####################################################################################
#
#   Group g 4, 9
#
